tool for generating common types of blocks inside minecraft datapack.

- place project folder inside resources (where data and assets are located).
- configs (datapack_context.json, main.js -> assetConfig) need to be configured.
- run from project root (where package.json is located), "npm start".